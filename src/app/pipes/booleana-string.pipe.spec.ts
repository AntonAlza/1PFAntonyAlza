import { BooleanaStringPipe } from './booleana-string.pipe';

describe('BooleanaStringPipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanaStringPipe();
    expect(pipe).toBeTruthy();
  });
});
