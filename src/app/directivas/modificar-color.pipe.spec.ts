import { ModificarColorPipe } from './modificar-color.pipe';

describe('ModificarColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ModificarColorPipe();
    expect(pipe).toBeTruthy();
  });
});
