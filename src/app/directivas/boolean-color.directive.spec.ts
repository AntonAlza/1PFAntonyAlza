import { BooleanColorDirective } from './boolean-color.directive';

describe('BooleanColorDirective', () => {
  it('should create an instance', () => {
    const directive = new BooleanColorDirective();
    expect(directive).toBeTruthy();
  });
});
